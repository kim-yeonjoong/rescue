{
  "name": "rescue",
  "version": "0.1.10",
  "private": true,
  "scripts": {
    "build": "swift build",
    "test": "swift test",
    "lint": "swiftlint lint --strict",
    "dev": "swift run Rescue",
    "build:swift": "swift build -c release"
  }
}
